#include <stdio.h>
#include <string.h>
#include <stdlib.h>

struct mahasiswa{
    char nim[10];
    char nama[50];
};

int main(){

    struct mahasiswa* ppti = (struct mahasiswa*) malloc (sizeof(struct mahasiswa));


    
}